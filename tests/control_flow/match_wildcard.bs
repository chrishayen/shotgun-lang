// Test: Wildcard pattern in match expression
fn get_value(int x) str {
    return match x {
        1 -> { "one" }
        2 -> { "two" }
        _ -> { "other" }
    }
}

fn main() {
    str a = get_value(1)
    str b = get_value(2)
    str c = get_value(99)

    print("{a}, {b}, {c}")
}
