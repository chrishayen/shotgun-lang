// Test: String literals and interpolation
fn main {
    str simple = "Hello"
    str name = "World"
    str greeting = "{simple}, {name}!"

    int x = 42
    str with_int = "The answer is {x}"

    print(greeting)
    print(with_int)
}
