// Test: Generic struct with multiple fields using type param

Container<T> :: struct {
    first T
    second T
    count int
}

fn main {
    Container<int> c = Container<int> {
        first: 10,
        second: 20,
        count: 2
    }

    int a = c.first
    int b = c.second
    int n = c.count
    int sum = a + b

    if a == 10 and b == 20 and n == 2 and sum == 30 {
        print("generic multi field test passed")
    } else {
        print("FAIL: field values incorrect")
    }
}
