// Test: Methods on generic structs with additional parameters

Box<T> :: struct {
    value T
}

Box<T> :: add(self, T other) T {
    return self.value + other
}

Box<T> :: get(self) T {
    return self.value
}

fn main {
    Box<int> b = Box<int> { value: 10 }
    int sum = b.add(32)
    int x = b.get()
    print("generic method with params test passed")
}
